{
  "name": "context-shrink",
  "version": "1.0.0",
  "description": "Scan, scrub, and shrink your code before sending to AI — save tokens, money, and prevent secret leaks.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "s:server": "cd server && npm run dev",
    "s:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "check-build": "node check-build.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}